// 이모지 선택기 초기화
const picker = new EmojiMart({
  // 이모지가 삽입될 입력란 선택자 지정
  input: "#emojiInput",
  // 이모지 패널이 나타날 위치 지정
  position: "top-start",
  // 이모지 패널의 사이즈 설정
  size: 240,
  // 이모지 선택기 테마 설정
  theme: "auto",
});
